<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:ChronoDSTranslator.Views.Controls">
    <Design.PreviewWith>
        <controls:SettingItem Title="Title" Description="Description">
            <TextBox Text="Content" />
        </controls:SettingItem>
    </Design.PreviewWith>

    <Style Selector="controls|SettingItem">
        <!-- Set Defaults -->
        <Setter Property="Template">
            <ControlTemplate>
                <Border>
                    <StackPanel Orientation="Vertical" Background="Transparent">
                        <TextBlock Classes.Title="True" Text="{TemplateBinding Title, Mode=OneWay}" />
                        <TextBlock Classes.Description="True" Text="{TemplateBinding Description, Mode=OneWay}" FontSize="{DynamicResource SubtitleFontSize}" Padding="0 4 0 0" />
                        <ContentPresenter Content="{TemplateBinding Content}" Padding="0 8 0 0" />
                    </StackPanel>
                </Border>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:pointerover /template/ Border">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundPointerOver}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrushPointerOver}" />
        </Style>
        <Style Selector="^:pointerover /template/ StackPanel">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundPointerOver}" />
        </Style>
    </Style>
</Styles>